// load sites and catalogs

function loadSites(options) {
  console.log('loading the sites...');
  console.log(options);
  var sites = {};

  return sites;
}

function loadCatalogs(options) {
  console.log('loading the catalogs...');
  console.log(options);
  var catalogs = {};

  return catalogs;
}

module.exports = { 'loadSites': loadSites, 'loadCatalogs': loadCatalogs };
