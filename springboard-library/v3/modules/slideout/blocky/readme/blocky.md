***Slideout Module***
---
*Default Theme*

Syntax highlighting

``` js
var foo = function (bar) {
  return bar++;
};

var settings = {
	width: 300
}
this.importer.include('slideout', settings);

console.log(foo(5));
```
